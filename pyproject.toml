[project]
name = "ai-resume-evaluator"
version = "0.1.0"
description = "AI-powered Resume Evaluator using Groq LLM"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "groq",
    "python-dotenv",
    "pydantic",
    "pypdf",
    "python-docx"
]
